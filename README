# Scaffold builder for alpine based docker single app image

It generates repo with gh actions workflow, Dockerfile and readme

## Run

To build new app

```
./build.sh app_name
```

It will create new app in scaffold directory. Copy it to new location